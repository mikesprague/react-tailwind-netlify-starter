[template]

[build]
  command = "npm run build"
  functions = "lambda"
  publish="build"

[dev]
  command = "npm start"
  port = 3000
  publish = "build"
  functionsPort = 9000

[production]
  command = "npm run build"
  publish = "build"
